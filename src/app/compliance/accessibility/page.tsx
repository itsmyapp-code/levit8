import ComplianceLayout from '@/components/layout/ComplianceLayout';

export default function Accessibility() {
  return (
    <ComplianceLayout 
      title="Accessibility Statement"
      content={<p>LEVIT8 is designed with high contrast and screen reader support in mind, ensuring all musicians can identify their music.</p>}
    />
  );
}
